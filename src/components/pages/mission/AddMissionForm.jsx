import Modal from "../../UI/Modal";

const AddMissionForm = (props) => {
  return (
    <Modal onCloseModal={props.onformClose}>
      <p>this is add mission form</p>
    </Modal>
  );
};
export default AddMissionForm;
